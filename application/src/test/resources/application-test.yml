spring:
  main:
    allow-bean-definition-overriding: true
  jpa:
    show-sql: true
  liquibase:
    enabled: true
    contexts: test
    change-log: classpath:db/changelog-test.xml
  properties:
    hibernate:
      format_sql: true
  docker:
    compose:
      enabled: true
      file: ../docker-compose.yml
      lifecycle-management: start-and-stop
      skip:
        in-tests: false
      stop:
        command: down
        arguments: -v
        timeout: 60s


logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: TRACE
    com.bullit.application.streaming: DEBUG