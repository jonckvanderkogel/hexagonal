spring:
  main:
    allow-bean-definition-overriding: true
  jpa:
    show-sql: true
  liquibase:
    contexts: test
  properties:
    hibernate:
      format_sql: true

logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: TRACE